en:
  site_settings:
    kofi_webhook_token: 'Verification token for webhook calls made by Ko-fi. 
      Can be found <a href="https://ko-fi.com/manage/webhooks" target="_blank">in the Ko-fi API settings</a>.'
    kofi_account: 'Your ko-fi account name, will be used to create a link on the dashboard'
    kofi_dashboard_enabled: 'Enable the dashboard/leaderboard of most recent donations at <code>/ko-fi</code>'
    kofi_dashboard_in_menu: 'Adds an item to the navigation sidebar. Disable this if you want to customize the link to the dashboard'
    kofi_dashboard_totals_types: 'Show totals for these payment types.'
    kofi_dashboard_totals_days: 'Number of days to look back. -1 means since ever'
    kofi_dashboard_count: 'How many of the recent payments to show. 0 will disable recent payments'
    kofi_dashboard_types: 'Which payment types should be shown on the dashboard'
    kofi_dashboard_anonymous_view: 'What details should be shown for unauthenticated users'
    kofi_dashboard_authenticated_view: 'What should be shown for authenticated users'
    kofi_leaderboard_count: 'Number of donators to show. 0 will disable the leaderboard'
    kofi_leaderboard_days: 'Number of days to consider for the leaderboard. -1 means since ever'
    kofi_leaderboard_types: 'Which payment types to consider for the leaderboard'
  kofi:
    payment_type:
      donation: Donation
      subscription: Subscription
      commission: Commission
      shop_order: Shop Order
    webhook:
      status:
        invalid_token: 'Received message %{msg_id} with invalid webhook token: %{verification_token}.'
        test_transaction: 'Received a test transaction, message %{msg_id}.'
        duplicate_message: 'Received message %{msg_id} for transaction: %{tx_id}. Duplicate message.'
        known_transaction: 'Received message %{msg_id} for transaction: %{tx_id}. Known transaction.'
        message_received: 'Received message %{msg_id} for transaction: %{tx_id}.'
    payments:
      import:
        invalid_csv: 'Failed loading CSV file. %{error}'
        tx_already_known: 'Transaction already registered'
        test_transaction: 'Test transaction ID'
        invalid_datetime: 'Invalid date/time: %{datetime}'
        invalid_type: 'Invalid transaction type: %{type}'
        invalid_amount: 'Invalid amount: %{amount}'
        zero_amount: 'Zero or negative amount'
        missing_tier_name: 'Subscription payment requires an item value as tier name'
      validation:
        test_transaction: 'test transaction cannot be stored'
    rewards:
      validation:
        payment_types_array: 'must be an array'
        invalid_payment_type: "'%{type}' is not a valid payment type"
  reports:
    kofi_payments:
      title: Ko-fi Payments
      description: Number of payments per type.
    kofi_payment_amount:
      title: Ko-fi Payment Amount
      description: Total amount receive per each type.
